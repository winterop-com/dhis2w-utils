== Suppressed Messages ==

# OIDs serve OID-based terminology exchange (CDA); a DHIS2-derived IG identifies everything by URI and UID.
%should have an OID assigned to cater for possible use with OID based terminology systems%
%could usefully have an OID assigned%

# Inherited from the core Location definition's own binding, not declared by anything in this IG.
%is deprecated with the note: 'Use additionalBinding extension or element instead'%
