name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege token: the job only reads the checkout, runs lint/test/docs.
permissions:
  contents: read

jobs:
  lint-test-docs:
    name: lint + test + docs
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install Python (pinned to 3.13 — matches CLAUDE.md)
        run: uv python install 3.13

      - name: Sync workspace
        run: make install

      - name: Lint (ruff + mypy + pyright)
        run: make lint

      - name: Check per-version example sync (v41/v43 mirror v42)
        run: make check-examples

      - name: Test + coverage (fast suite, excludes @pytest.mark.slow)
        run: make coverage

      - name: Upload coverage.xml
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: error
          retention-days: 14

      - name: Build docs (mkdocs)
        run: make docs-build
