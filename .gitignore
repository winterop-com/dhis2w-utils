# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# mkdocs output
/site/

# Frontend (packages/dhis2w-fhir-serve/frontend)
node_modules/
# The built capture UI. Unlike a repo that commits its bundle, this workspace
# publishes wheels from CI, and CI runs `make build-frontend` before packaging -
# so a committed copy would only be the same bytes going stale between rebuilds.
# The directory itself is kept by a .gitkeep the vite build rewrites.
packages/dhis2w-fhir-serve/src/dhis2w_fhir_serve/static/*
!packages/dhis2w-fhir-serve/src/dhis2w_fhir_serve/static/.gitkeep

# mypy / pyright / ruff caches
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
.pyright/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Databases
*.db
*.sqlite
*.sqlite3
.dhis2/tokens.sqlite*
.dhis2/cache.sqlite*

# Project-local DHIS2 config (we never commit profiles)
.dhis2/profiles.toml

# Jupyter
.ipynb_checkpoints

# oh-my-claudecode session state
.omc/

# Security audit scanner report bundles (timestamped output directories)
dhis2-security-*/

# Playwright MCP session artefacts (per-run console logs + page snapshots)
.playwright-mcp/
.claude/
*.png
# Committed brand assets — applied by the e2e seed via `Dhis2Client.customize`
!infra/login-customization/*.png
# Committed report + sharing explorer bundle assets — shipped as package data,
# copied into each run folder
!packages/dhis2w-core/src/dhis2w_core/security_core/report/assets/*.png
!packages/dhis2w-core/src/dhis2w_core/security_core/sharing/assets/*.png
